<!DOCTYPE html>
<html>
  <head>
    <title><%= title %></title>
    <link rel='stylesheet' href='/stylesheets/style.css' />
  </head>
  <script src="/socket.io/socket.io.js"></script>
  <script src="http://code.jquery.com/jquery-1.12.0.js"></script>
  <script>
  		var socket = io();
  		$('form#form').submit(function(e){
  			alert("aaaaaa");
  			e.preventDefault();
  			socket.emit('hahahah',{my:'data'/*ip:$('#ip').val(),reg:$('#reg').val(),no:$('#no').val()*/});
  			$('#ip').val('');
  			$('#reg').val('');
  			$('#no').val('');
  			return false;
  		})

  		/*var refreshld = setInterval(function(){
  			socket.emit('refresh');
  		},1000)*/
  </script>
  <body>
    <h1><%= title %></h1>
    <form id="form">
    	<input id="ip" autocomplete="off">
    	<input id="reg" autocomplete="off">
    	<input id="no" autocomplete="off">
    	<input type="submit" value="Send">
    </form>
    <p>Welcome to <%= title %></p>
    <ul id="regiester"></ul>
  </body>
</html>
